const user = {
    name: "Domami Pandu Saputra",
    age: 22,
    address: "Jakarta Selatan",
    isMarried: true,
};

const { name, age, address } = user;

console.log(name);
console.log(age);
console.log(address);